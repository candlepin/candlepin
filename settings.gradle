pluginManagement {
    repositories {
        mavenCentral()
        gradlePluginPortal()
        mavenLocal()
    }
    plugins {
        id "io.quarkus" version "2.16.4.Final"
    }
}

rootProject.name = "candlepin"
include 'client'
include 'spec-tests'

