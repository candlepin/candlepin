# Setup

```
# Pipenv for dependency and virtualenv management
dnf install pipenv

# Install dependencies from Pipenv.lock
pipenv sync

# Run the script within the virtualenv
pipenv run ./org_migrator --help
```
