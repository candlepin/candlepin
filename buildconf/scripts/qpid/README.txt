If you are installing CanadianTenPin with a standalone qpid broker then run:

./configure-qpid.sh

setup.sh is a subset of make_keys.sh and can be used to configure canadianTenPin to
work with a qpid broker installed by pulp.
